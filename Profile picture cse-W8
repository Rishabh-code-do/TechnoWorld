#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int l;
    scanf("%d",&l);
    int n;
    scanf("%d",&n);
    while(n--)
    {
        int w,h;
        scanf("%d %d",&w,&h);
        if(l>w||l>h)
            printf("UPLOAD ANOTHER\n");
        else
        {
            if(w>l||h>l)
                printf("CROP IT\n");
            else
                printf("ACCEPTED\n");
        }
            
    }
    return 0;
}
